const computeRRule = ({
  repeatFrequency,
  repeatMode,
  onMonth,
  onDay,
  onThe,
  onTheDay,
  onTheMonth,
  end,
  endDate,
}) => {
  // do some magic
  return end + endDate;
};

export default computeRRule;
